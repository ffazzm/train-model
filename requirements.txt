# =========================================================================
# Core (PyTorch) — used by image classification
# For GPU, install a CUDA build from https://pytorch.org instead of these.
# =========================================================================
# torch==2.5.1
# torchvision==0.20.1

# Image Classification (timm pretrained models)
timm==1.0.12

# ONNX export + drift verification (model_converter.py)
onnx==1.17.0
onnxruntime==1.27.0

# YOLO Object Detection (Ultralytics)
# ultralytics==8.3.55

# PaddleOCR
# Install PaddlePaddle first (choose CPU or a matching CUDA build):
#   CPU : pip install paddlepaddle==2.6.2
#   GPU : pip install paddlepaddle-gpu==2.6.2 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
# paddleocr==2.9.1

# Shared utilities
PyYAML==6.0.2
tqdm==4.67.1
Pillow==11.0.0
